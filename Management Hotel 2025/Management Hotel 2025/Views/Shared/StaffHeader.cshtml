@{
    Layout = null;
    var title = ViewData["Title"] ?? "Hotel Management";
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@title - Hotel Management</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

    <!-- FontAwesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" rel="stylesheet" />

    <style>
        body {
            background-color: #f8f9fa;
        }

        .navbar-brand {
            font-weight: bold;
        }
    </style>
</head>
<body>

    <!-- HEADER / NAVBAR -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="@Url.Action("Index", "Home")">
                <i class="fas fa-hotel me-2"></i> Hotel Management
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarNav" aria-controls="navbarNav"
                    aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">

                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Index", "Home")">
                            <i class="fas fa-file-invoice-dollar me-1"></i> Danh sách phòng
                        </a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("ViewMapOfRoom", "StaffManagementRoom")">
                            <i class="fas fa-bed me-1"></i> Sơ đồ phòng
                        </a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" target="_blank" href="@Url.Action("CheckInPassengers", "StaffManagementRoom")">
                            <i class="fas fa-calendar-plus me-1"></i> Nhận phòng
                        </a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Cancel", "Bookings")">
                            <i class="fas fa-ban me-1"></i> Quản lý phòng
                        </a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("CheckIn", "Bookings")">
                            <i class="fas fa-sign-in-alt me-1"></i> Quản lý dịch vụ
                        </a>
                    </li>

                  
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("BookingsView", "StaffManagementRoom")">
                            <i class="fas fa-sign-in-alt me-1"></i> Quản lý booking
                        </a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("ViewDetailBooking", "StaffManagementRoom")">
                            <i class="fas fa-file-invoice-dollar me-1"></i> xem chi tiết booking 
                        </a>
                    </li>

                </ul>

                <!-- USER MENU -->
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("SignOut", "Authen")">
                            <i class="fas fa-user-circle me-1"></i> Tài khoản
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("SignOut", "Authen")">
                            <i class="fas fa-right-from-bracket me-1"></i> Đăng xuất
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- MAIN CONTENT -->
    <div class="container-fluid mt-4">
        @RenderBody()
    </div>
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    @RenderSection("Scripts", required: false)
</body>
</html>