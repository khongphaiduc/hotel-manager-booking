
@model IEnumerable<Management_Hotel_2025.ViewModel.ViewRoomModel>

@{
    Layout = "~/Views/Shared/AdminSidebar.cshtml";

}

@* Phần CSS tùy chỉnh để tinh chỉnh khoảng cách 
  Bạn có thể đặt phần này trong tệp .css riêng của mình 
*@
<style>
    .filter-card {
        margin-bottom: 20px;
    }

    .action-bar {
        margin-bottom: 15px;
    }

    .action-buttons .btn {
        margin-right: 5px; /* Thêm khoảng cách nhỏ giữa các nút Sửa/Xóa */
    }
</style>

<div class="container-fluid mt-4">



    <div class="card shadow-sm filter-card">
        <div class="card-body">


            @* Form này sẽ gửi dữ liệu lọc về action Index bằng phương thức GET *@
            <form asp-action="Index" method="get">
                <div class="row g-3 align-items-end">

                    <div class="col-md-3">
                        <label for="floorFilter" class="form-label">Lọc theo tầng:</label>
                        <select id="floorFilter" name="floor" class="form-select" asp-items="ViewBag.Floors">
                            <option value="">Tất cả các tầng</option>
                        </select>
                    </div>

                    <div class="col-md-3">
                        <label for="statusFilter" class="form-label">Lọc theo trạng thái:</label>

                        <select id="statusFilter" name="status" class="form-select" asp-items="ViewBag.Statuses">
                            <option value="">Tất cả trạng thái</option>
                        </select>
                    </div>

                    <div class="col-md-2">
                        <button type="submit" class="btn btn-primary w-100">
                            <i class="bi bi-filter"></i> Lọc
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <div class="action-bar text-end">
        <a asp-action="Create" class="btn btn-success">
            <i class="bi bi-plus-circle"></i> Thêm phòng mới
        </a>
    </div>

    <div class="card shadow-sm">
        <div class="card-header">
            <h5 class="mb-0">Danh sách phòng</h5>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover table-bordered align-middle">
                    <thead class="table-dark">
                        <tr>
                            <th scope="col" style="width: 5%;">STT</th>
                            <th scope="col">Số phòng</th>
                            <th scope="col">Loại phòng</th>
                            <th scope="col">Giá (VNĐ)</th>
                            <th scope="col">Trạng thái</th>
                            <th scope="col" style="width: 12%;">Thao tác</th>
                        </tr>
                    </thead>
                    <tbody>
                        @* Kiểm tra xem Model có dữ liệu không *@
                        @if (Model == null || !Model.Any())
                        {
                            <tr>
                                <td colspan="6" class="text-center">Không tìm thấy dữ liệu phòng.</td>
                            </tr>
                        }
                        else
                        {
                            int stt = 1;
                            foreach (var item in Model)
                            {
                                <tr>
                                    <td>@(stt++)</td>
                                    <td>@Html.DisplayFor(modelItem => item.NumberOfRooms)</td>
                                    <td>@Html.DisplayFor(modelItem => item.Name)</td>


                                    <td>@item.Price.ToString("N0")</td>

                                    <td>
                                        @* Sử dụng badge (nhãn) của Bootstrap để làm nổi bật trạng thái
                                          Bạn có thể tùy chỉnh logic này
                                        *@
                                        @if (item.status == "Còn trống")
                                        {
                                            <span class="badge bg-success">Còn trống</span>
                                        }
                                        else if (item.status == "Đang có khách")
                                        {
                                            <span class="badge bg-danger">Đang có khách</span>
                                        }
                                        else if (item.status == "Đang dọn dẹp")
                                        {
                                            <span class="badge bg-warning text-dark">Đang dọn dẹp</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary">@item.status</span>
                                        }
                                    </td>

                                    <td class="action-buttons">
                                        @* Các nút Sửa/Xóa. asp-route-id truyền ID của phòng đến Controller *@
                                        <a asp-controller="Admin" asp-action="AdjustRoom" asp-route-idRoom="@item.IdRoom" class="btn btn-sm btn-outline-primary" title="Sửa">
                                            <i class="bi bi-pencil-square"></i>
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@item.IdRoom" class="btn btn-sm btn-outline-danger" title="Xóa">
                                            <i class="bi bi-trash"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@* GHI CHÚ QUAN TRỌNG:
  1. Để các icon (bi-filter, bi-plus-circle...) hiển thị, bạn cần thêm thư viện 
     Bootstrap Icons vào tệp _Layout.cshtml của mình.
     <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">

  2. Code này giả định bạn đang sử dụng Bootstrap 5.

  3. Bạn cần thay thế 'YourProject.Models.RoomViewModel' bằng tên Model bạn dùng.
     Model này nên có các thuộc tính như: Id, RoomNumber, RoomType, Price, Status.

  4. Các thẻ 'asp-action' và 'asp-route-id' là các Tag Helper của ASP.NET Core, 
     chúng sẽ tự động tạo link (href) đến các Action trong Controller của bạn.
     Ví dụ: asp-action="Create" sẽ trỏ đến hàm Create() trong Controller.
*@